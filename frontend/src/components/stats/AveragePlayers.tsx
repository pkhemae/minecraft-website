import React from 'react';

const Footer: React.FC = () => {
    return (
        <>
        {/* TODO: Make the component functionnal with mc-stats.com */}
        <span>200</span>
        </>
    );
};

export default Footer;